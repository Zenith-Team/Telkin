# Mod Management
The @ref tk::getMods() function provides an interface to inspect the list of loaded mods, returning an array of @ref tk::ModInfo objects.

Additionally, @ref tk::isModLoaded() is available to directly query for the presence of a specific mod.
